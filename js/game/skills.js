class SkillHtml extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/html-5-icon.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillCss extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/css-3-icon.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillSass extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/sass-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillBootstrap extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/bootstrap-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillJquery extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/jquery-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillReact extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/react-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillAngular extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/angular-logo.svg",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillNode extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/nodejs-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillMeteor extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/meteor-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}

class SkillMongo extends Skill {
  constructor(props) {
    super(
      Object.assign({}, props, {
        src: "./assets/images/mongodb-logo.png",
        width: props.width || 50,
        height: props.height || 50
      })
    );
  }
}
